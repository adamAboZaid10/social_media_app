import 'package:flutter/material.dart';

import '../../widget/widget_room_space_screen/open_room_space_screen_body.dart';

class OpenRoomSpaceScreen extends StatelessWidget {
  const OpenRoomSpaceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: OpenRoomSpaceScreenBody(),
    );
  }
}

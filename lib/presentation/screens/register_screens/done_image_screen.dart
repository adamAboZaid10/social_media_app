import 'package:flutter/material.dart';

import '../../widget/widget_register_screen/done_image_screen_body.dart';

class DoneImageScreen extends StatelessWidget {
  const DoneImageScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: DoneImageScreenBody(),
    );
  }
}
